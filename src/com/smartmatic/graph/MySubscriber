package com.smartmatic.graph;

public class MySubscriber extends Subscriber {

  private Session session;
  
  private String regionName;
  
  public MySubscriber(Session session) {
      this.session = session;
  }
  
  public void onComplete() {}
  
  public void onNext() {
      session.getBasicRemote().sendMessage();
  }
  public void onError() {}
  
  public static class SubscriberComparator implements Comparator<MySubscriber> {
  
      public int compare(MySubscriber obj1, MySubscriber obj2) {
            return obj1.regionName.compare(obj2.regionName);
      }
  
  }
  
}
